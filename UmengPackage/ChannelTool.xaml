<UserControl x:Class="UmengPackage.ChannelTool"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:UmengPackage"
             Name="tool"
             Height="480" Width="800"
             Background="#242328">
    <UserControl.Resources>
        <Style x:Key="cyan_button_style" TargetType="{x:Type Button}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="border">
                                            <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Opacity)" Storyboard.TargetName="border">
                                            <EasingDoubleKeyFrame KeyTime="0" Value="0.8"/>
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled"/>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border x:Name="border" BorderBrush="{x:Null}" BorderThickness="1" Height="Auto" Margin="0" 
                                VerticalAlignment="Stretch" Width="40" CornerRadius="5" Opacity="0">
                            <Border.Background>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#00000000" Offset="1"/>
                                    <GradientStop Color="#FFD61717"/>
                                    <GradientStop Color="#81850909" Offset="0.569"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Border BorderThickness="1" CornerRadius="5" BorderBrush="Black" Background="#23AEEF">
                            <Border BorderThickness="1" CornerRadius="5" BorderBrush="#54CCFE" Background="Transparent">
                                <Border BorderThickness="0,0,0,0" CornerRadius="5">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                            <GradientStop Color="#FF616166" Offset="0"/>
                                            <GradientStop Color="#004EA4F3" Offset="1"/>
                                            <GradientStop Color="#FFDAE2EB" Offset="0.522"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <Rectangle Margin="-0.2,1,0,0" Height="46" VerticalAlignment="Top" Fill="#FF4999CB"></Rectangle>
                                </Border>
                            </Border>
                        </Border>
                        </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True"/>
                        <Trigger Property="IsDefaulted" Value="True"/>
                        <Trigger Property="IsMouseOver" Value="True"/>
                        <Trigger Property="IsPressed" Value="True"/>
                        <Trigger Property="IsEnabled" Value="False"/>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    </UserControl.Resources>
    <!--  xmlns:local="clr-namespace:UmengPackage"
             xmlns:common_ui="clr-namespace:UIControls;assembly=UIControls"
             xmlns:converter="clr-namespace:UmengPackage.Source.Model"-->

    <!--UserControl.Resources>
       
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="HorizontalListDictionary.xaml"/>
                <ResourceDictionary Source="/UIControls;component/CommonStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <converter:StatusToColor x:Key="stateToColor" />
        </ResourceDictionary>
    </UserControl.Resources-->
    <Grid Height="442">
        <!--Canvas Height="150" HorizontalAlignment="Left" Margin="12,278,0,0" Name="image1" VerticalAlignment="Top" Width="200" 
                AllowDrop="True" Background="#FF89ADD1"
                DragDrop.DragEnter="dragDrop_Event" 
                DragDrop.Drop="dragDrop_Event"
                DragDrop.DragOver="dragDrop_Event"
                DragDrop.DragLeave="dragDrop_Event">

            <Label Name="hint" Canvas.Left="40" Canvas.Top="50"/>
            <Image Source="{Binding AppIcon}" Canvas.Left="40" Canvas.Top="50"/>
        </Canvas-->
        <!--ComboBox Name="settingList"  Height="23" HorizontalAlignment="Left" Margin="611,2,0,0" VerticalAlignment="Top" Width="120"
                  SelectedIndex="0"
                  >
        </ComboBox>
        <Button Content="Edit" Height="23" HorizontalAlignment="Left" Margin="739,2,0,0" Name="button1" VerticalAlignment="Top" Width="38" Click="edit_Click" /-->
        <ListBox HorizontalAlignment="Left" VerticalAlignment="Top" Width="740" Height="200" x:Name="Channels" Background="Transparent" Margin="30,1,2,1" >
            
            <ListBox.Template>
                <ControlTemplate>
                    <Border Background="#35333D" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                        <Border Background="Transparent" BorderBrush="#383543" BorderThickness="1" ClipToBounds="True" CornerRadius="5">
                            <Border Background="Transparent" BorderBrush="#26242C" BorderThickness="3,3,3,3" Margin="-3,-3,-3,-3" CornerRadius="5">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="0" BlurRadius="15"/>
                                </Border.Effect>
                            </Border>
                        </Border>
                    </Border>
                </ControlTemplate>
            </ListBox.Template>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            
            <ListBox.ItemTemplate>
                <DataTemplate >
                    <Border Width="100" Height="60">
                        <TextBlock  Text="{Binding ChannelName}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#FFEB3030" />
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="一键打包" Height="49" HorizontalAlignment="Left" Margin="585,364,0,0" Name="btn_builder" 
                VerticalAlignment="Top" Width="151" Style="{StaticResource cyan_button_style}"/>
       
        
        <!--GroupBox Header="Apk Info" Height="132" HorizontalAlignment="Left" Margin="218,298,0,0" Name="groupBox1" VerticalAlignment="Top" Width="239">
            <Grid>
                <TextBlock Text="{Binding AppName}" Height="23" HorizontalAlignment="Left" Margin="6,13,0,0"  VerticalAlignment="Top" />
                <TextBlock Text="{Binding AppVersionName}" Height="23" HorizontalAlignment="Left" Margin="6,42,0,0"  VerticalAlignment="Top" />
                <TextBlock Text="{Binding AppSize}" Height="23" HorizontalAlignment="Left" Margin="6,66,0,0"  VerticalAlignment="Top" />
            </Grid>
        </GroupBox-->

        <Grid Width="400" Height="180" Margin="10,223,350,19">
            <Border Background="#35333D" BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                <Border Background="Transparent" BorderBrush="#383543" BorderThickness="1" ClipToBounds="True" CornerRadius="5">
                    <Border Background="Transparent" BorderBrush="#26242C" BorderThickness="3,3,3,3" Margin="-3,-3,-3,-3" CornerRadius="5">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="0" BlurRadius="15"/>
                    </Border.Effect>
                </Border>
                </Border>
            </Border>
            
            
        </Grid>
    </Grid>
</UserControl>
 